services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: "93953"
      POSTGRES_DB: "project"

  frontend:
    build: .
    volumes:
      - .:/app
    ports:
      - "0.0.0.0:8000:3000"
    depends_on:
      - postgres