services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: "93953"
      POSTGRES_DB: "project"

  frontend:
    build: .
    volumes:
      - .:/app
    command: npm run dev
    ports:
      - "3000:3000"
    depends_on:
      - postgres